# {{ ansible_managed }}

description "{{ project_name }}"

start on (filesystem)
stop on runlevel [016]

respawn
setuid www-data
setgid www-data
chdir /var/www/{{ website }}

exec /home/deploy/.virtualenv/bin/gunicorn {{ project_name }}.wsgi:application -b 127.0.0.1:8000
